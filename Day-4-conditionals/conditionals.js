/*********************
 * Conditions can be implemented using the following ways:
 * - if
 * - if else
 * - if else if else
 * - switch
 * - ternary operator
 */

// if
/*****************
 * synthax
 * if (condition) {
 *      //this part of code executes
 * }
 */

{
  let num = 3;
  if (num > 0) {
    console.log(`${num} is a positive number.`);
  }
}

{
  let isRaining = true;
  if (isRaining) {
    console.log(`Remember to take your rain coat.`);
  }
}
